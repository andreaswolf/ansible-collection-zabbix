# Setup: Manage basic resource installation, such as packages or service users.

---

- name: "Setup | Install | Default | Remove unwanted packages (e.g., to prevent conflicts or resolve incompatibilities)"
  ansible.builtin.package:
    name: "{{ __server_zabbix_packages_removal }}"
    state: "absent"
  when:
    - __server_zabbix_packages_removal is defined
    - (__server_zabbix_packages_removal | length) > 0


- name: "Setup | Install | Default | Install or upgrade packages"
  ansible.builtin.package:
    name: "{{ __server_zabbix_packages_install }}"
    state: "{{ 'latest' if server_zabbix_autoupgrade else 'present' }}"
  when:
    - __server_zabbix_packages_install is defined
    - (__server_zabbix_packages_install | length) > 0
