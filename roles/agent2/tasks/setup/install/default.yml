# Setup: Manage basic resource installation, such as packages or service users.

---

- name: "Setup | Install | Default | Remove unwanted packages (e.g., to prevent conflicts or resolve incompatibilities)"
  ansible.builtin.package:
    name: "{{ __agent2_zabbix_packages_removal }}"
    state: "absent"
  when:
    - __agent2_zabbix_packages_removal is defined
    - (__agent2_zabbix_packages_removal | length) > 0


- name: "Setup | Install | Default | Install or upgrade packages"
  ansible.builtin.package:
    name: "{{ __agent2_zabbix_packages_install }}"
    state: "{{ 'latest' if agent2_zabbix_autoupgrade else 'present' }}"
  when:
    - __agent2_zabbix_packages_install is defined
    - (__agent2_zabbix_packages_install | length) > 0
